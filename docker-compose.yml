services:
  service1:
    build: ./service1
    image: kerdamon/service1
  api-gateway:
    build:
      context: ./api-gateway
    image: kerdamon/api-gateway
    ports:
      - "443:443"
      - "80:80"
    depends_on:
      - service1
